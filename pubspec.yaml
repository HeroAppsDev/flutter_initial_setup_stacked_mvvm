name: initial_setup_flutter_stacked_mvvm
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  #state management
  stacked: ^2.2.5

  #services - navigations, dialogs, etc.
  stacked_services: ^0.8.13

  #http client
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner: ^2.1.2
  stacked_generator: ^0.5.2

flutter:
  uses-material-design: true

#Command to generate stacked_services - flutter pub run build_runner build
